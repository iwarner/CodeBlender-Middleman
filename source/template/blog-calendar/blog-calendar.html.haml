---

meta :
    author      : "DryKISS"
    description : "Blog calendar, articles by date - CodeBlender"
    image       : "Image"
    subject     : "DryKISS"
    title       : "Blog calendar, articles by date - CodeBlender"
    type        : "website"

heading :
    small : Articles by date
    title : Calendar

layout   : "tag"
pageable : true
per_page : 5

---

-# Blog calendar
.blogCalendar

    -# Article list
    .blogCalendar__list
        = codeBlender "article-list", "template", locals: { page_articles: page_articles }

    -# Pagination variable
    - prevPage = prev_page ? prev_page : false
    - nextPage = next_page ? next_page : false

    -# Pagination
    .blogCalendar__pagination
        = codeBlender "pagination", "molecule", { nextPage: nextPage, prevPage: prevPage, pageNo: page_number, pages: num_pages }

---

# Angular
angular :
    controller   : PortfolioController
    controllerAs : portfolio

description : "Portfolio template"
title       : "Portfolio template"

# Tasks
todo :
    - - "Add a tooltip with the total number of posts in each section - on hover"
    - - "Find a way to include the language elements from this module also"
    - - "Sort out the design of the blockquote, caption and thumbnail image"
    - - "Use the link_to feature for the links"

# Resource
see :
    - - "Mix it up"
      - "https://mixitup.kunkalabs.com/"
    - - "Keen themes portfolio"
      - "http://keenthemes.com/preview/metronic/theme/admin_1/page_general_portfolio_1.html"

---

-# Bootstrap row
.row

    -# Bootstrap column
    .col-md-6

        -# Panel body
        - panelBody = capture_html do

            -# Markdown
            :markdown

                Creates a portfolio system from a the blog setup. Including :

                - Recent module to be utilised on the home page
                - A portfolio template that showcases all the work
                - Individual profile pages showcasing the detailed work
                - MixItUp filtering of the portfolio

        -# Panel
        = codeBlender "panel", "molecule", { body: panelBody, heading: { text: "Introduction" } }

    -# Bootstrap column
    .col-md-6

        -# Panel body - markdown
        - panelBody = capture_html do

            -# Markdown
            :markdown

                - Include all JS
                - Include All Style
                - Include the Partials in pages you want
                - Add the portfolio blog
                - Create the blog posts with the relevant front-matter

                Image sizes are based on the column width chosen. Make sure they are

                - ( 1170 - ( gutterWidth ( columns * 2 ) ) ) / columns
                - ( 1170 - ( 15 * ( columns * 2 ) ) ) / 3

        -# Panel
        = codeBlender "panel", "molecule", { body: panelBody, heading: { text: "Setup" } }

-# Bootstrap row
.row

    .col-md-12

        %br

        -# Default
        :ruby
            code = %{codeBlender "portfolio-recent", "template/portfolio", { total: 4 }}

        -# Code
        = codeBlender "code", "molecule", { code: code, heading: { text: "Recent" } }

        %hr

        -# Default
        :ruby
            code = %{codeBlender "portfolio", "template"}

        -# Code
        = codeBlender "code", "molecule", { code: code, heading: { text: "Portfolio" } }

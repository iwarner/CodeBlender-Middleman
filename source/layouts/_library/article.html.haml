-##
-# Blog Article
-#
-# @usage
-# -# Article layout
-# = wrap_layout "_library/article" do
-#
-# @author   Ian Warner <ian.warner@drykiss.com>
-# @category template
-#
-# @todo - Add in search - Add in tags - Add in social links - facebook like and share, tweet and pin - Add in report page button
-# @todo - Add in video / image - could be carousel - Add in title - Add in price - Add in content - Add in Ribbons on some products
-# @todo - Add in Trending Today - Add in Buy Now - Add in Add to Wishlist - Add in Comments - Add in related products - Add in newsletter
-##

-# Variables
- cp      = current_page.data
- heading = cp.heading

-# Article
= wrap_layout "_library/fullWidth" do

    .container

        .row

            .col-md-3

                %aside

                    -# Tags
                    .tagCloud

                        -# Tag Cloud
                        = partial "./molecule/tagCloud/tagCloud"

                    -# Latest Posts
                    .blogLatest

                        -# Recent Articles
                        = partial "./molecule/recent/recent"

            .col-md-6

                -# Blog Post
                %article{ role: "article", itemscope: true, itemtype: "http://schema.org/BlogPosting"  }

                    -# Heading - Article H1
                    = partial "./molecule/heading/heading", locals: { extraClass: "heading--noMargin", small: heading.small, text: heading.title, itemprop: "name headline" }

                    -# Blog Detail Footer
                    = partial "./molecule/detail/detail", locals: { author: true, cal: true, comment: true, social: true, tags: true }

                    -# Blog Image
                    = partial "./molecule/image/image"

                    -# Yield
                    %span{ itemprop: "text" }
                        = yield

                    -# Pager
                    = partial "./molecule/pager/pager"

                    -# Discus Comments
                    = partial "./molecule/disqus/comments"

            .col-md-3

                %aside

                    -# Blog Archive
                    = partial "./molecule/archive/archive"
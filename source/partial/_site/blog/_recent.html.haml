-##
-# Blog - Recent Articles
-#
-# Locals
-# count - INT - Optional - How many articles to display
-#
-# Usage
-# -# Recent Articles
-# = partial "blog/recent",
-#            :locals => { :count => "10" }
-#
-# @author Ian Warner <ian.warner@drykiss.com>
-##

-# Count Default
- if ! locals[ :count ]
    - count = 10

-# Heading
= partial "./bootstrap/type/heading",
           :locals => { :text => "Latest Posts",
                        :type => "h4" }

-# Latest
%ul.list-unstyled.labelList

    -# Loop through the Blog Posts
    - blog.articles[ 0...count ].each do | article |

        -# List
        %li

            -# Label
            = partial "./bootstrap/components/label",
                       :locals => { :color   => "info",
                                    :content => article.date.strftime( '%b %e' ) }

            -# Link to the Article
            = link_to article.title, article
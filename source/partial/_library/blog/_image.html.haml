-##
-# Blog Image
-#
-# @author Ian Warner <ian.warner@drykiss.com>
-##

-# Variables
- cp    = current_page.data
- image = cp.image
- title = cp.title

-# Check if current page has an image
- if image

    -# Look for an array
    - if image.is_a?( Array )

        -# Carousel
        = partial "./bootstrap/javascript/carousel",
                   locals: { id:        "carousel1",
                             array:     image,
                             indicator: :true }

    - elsif ! image.is_a?( Array )

        -# Image
        = partial "./bootstrap/css/image",
                   locals: { alt:        string( title ),
                             src:        image,
                             extraClass: "img-responsive" }
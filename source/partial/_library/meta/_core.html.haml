-##
-# Core Meta Tags
-#
-# Tags can be set via the current_page.data elements or through content yield the
-# latter is especially useful for localized pages where the front matter will
-# change dynamically for the language.
-#
-# @author Ian Warner <ian.warner@drykiss.com>
-##

-# Variables
- cp   = current_page
- dc   = data.config
- path = cp.path
- m    = cp.data.stub

-# Title
%title= string :"meta.#{ m }.title"

-# Encoding
%meta{ charset: "utf-8" }

-# Meta Tags for IE2
%meta{ content: "IE=edge,chrome=1", http: { equiv: "X-UA-Compatible" } }

-# Description
%meta{ name: "description", content: string( :"meta.#{ m }.description" ) }

-# Keywords
%meta{ name: "keywords", content: string( :"meta.#{ m }.description" ) }

-# Author
%meta{ name: "author", content: string( :"meta.#{ m }.author" ) }

-# Robots
%meta{ name: "robots", content: "index,follow,archive" }

-# CSRF Token
%meta{ name: "csrf-token", content: Random.new_seed }

-# Canonical
%meta{ rel: "canonical", href: dc.url + cp.url }
-##
-# Blog Recent Articles
-#
-# @usage
-# -# Blog Recent
-# = partial "./molecule/recent/recent"
-#
-# @category molecule
-# @author   Ian Warner <ian.warner@drykiss.com>
-##

-# Count Default
- if ! locals[ :count ]
    - count = 10

-# Latest
%ul.list-unstyled

    -# Loop through the Blog Posts
    - blog.articles[ 0...count.to_i ].each do | article |

        -# List
        %li

            -# Label
            = partial "./atom/label/label", locals: { context: "info", content: article.date.strftime( '%b %e' ) }

            -# Link to the Article
            = link_to string( article.data.heading.title ), article
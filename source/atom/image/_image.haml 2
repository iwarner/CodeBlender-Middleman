-##
-# Blog image
-#
-# @usage
-# -# Blog image
-# = codeBlender "image", "molecule"
-#
-# @author   Ian Warner <ian.warner@drykiss.com>
-# @category molecule
-##

-# Variables
- cp    = current_page.data
- image = cp.meta.image
- title = cp.title

-# Check if current page has an image
- if image

    -# Look for an array
    - if image.is_a?( Array )

        -# Carousel
        = codeBlender "carousel", "molecule", locals: { id: "carousel1", array: image, indicator: :true }

    - elsif ! image.is_a?( Array )

        -# Image
        = codeBlender "image", "atom", { alt: title, src: image, extraClass: "img-responsive" }

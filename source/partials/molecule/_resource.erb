-##
-# Resource
-# Allows for a list of Websites; links to designs and wireframe snippets.
-#
-# @author   Ian Warner <ian.warner@drykiss.com>
-# @category organism
-##

-# Variables
- cp = current_page.data
- dc = data.config

-# Check Array Exists
- if cp.see or locals[ :array ]

    -# Heading
    = codeBlender "heading", "molecule", { small: "Useful websites and tutorials", tag: "h3", text:  "Resources" }

    -# Create Array
    - array = { column: [ "Title", "URL" ], data: array ? array : cp.see }

    -# Table
    .table-responsive

        = codeBlender "table", "molecule", { array: array, extraClass: "table-striped table-condensed" }
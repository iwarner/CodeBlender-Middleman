---

# Meta
description : "Facebook share button"
title       : "Facebook share button"

# Resources
see :
    - - "Share button"
      - "https://developers.facebook.com/docs/plugins/share-button/"
    - - "Share dialog"
      - "https://developers.facebook.com/docs/sharing/reference/share-dialog"
    - - "Sharing for webmasters"
      - "https://developers.facebook.com/docs/sharing/webmasters"
    - - "Sharing best practices"
      - "https://developers.facebook.com/docs/sharing/best-practices"

# Todo
todo :
    - - "Create the ability to invoke the share dialog without the button : https://developers.facebook.com/docs/sharing/reference/share-dialog"
    - - "Error if a URL is not present - do not default"

---

-# Bootstrap row
.row

    -# Bootstrap column
    .col-md-6

        -# Panel body
        - panelBody = capture_html do

            -# Markdown
            :markdown

                The share button provides an easy to use access into the Facebook
                share dialog, as well as some other functionality such as the
                button counter.

                - Need to create an application AppID
                - Include the Facebook JS SDK
                - Note the language needs to be configured for the SDK
                - Include the code in the Angular run to load and init the SDK

        -# Panel
        = codeBlender "panel", "molecule", { body: panelBody, heading: { text: "Introduction" } }

    -# Bootstrap column
    .col-md-6

        -# Panel body - markdown
        - panelBody = capture_html do

            -# Markdown
            :markdown

                There are essentially three ways to call the share dialog. through
                the provided Facebook button, Javascript of a link.

                Can also invoke the share dialog directly through JavaScript,
                this has the added advantage of being able to be used on a
                custom button.

                - Trigger a Share Dialog using the FB.ui function with the share method parameter to share a link.
                - Need to implement

        -# Panel
        = codeBlender "panel", "molecule", { body: panelBody, heading: { text: "Setup" } }

%hr

-# Bootstrap row
.row

    .col-md-12

        -##
        -# Default
        -##
        :ruby
            code = %{codeBlender "share-button", "social/facebook/share-button", { href: "http://drykiss.com/" } }

        -# Code
        = codeBlender "code", "molecule", { code: code, heading: { text: "Default" } }

        -##
        -# Button count
        -##
        :ruby
            code = %{codeBlender "share-button", "social/facebook/share-button", { href: "http://drykiss.com/", layout: "button_count" } }

        -# Code
        = codeBlender "code", "molecule", { code: code, heading: { text: "Button count" } }

        %hr

        -##
        -# Button - share dialog
        -##
        :ruby
            code = %{codeBlender "share-button", "social/facebook/share-button", { href: "http://drykiss.com/", type: "javascript" } }

        -# Code
        = codeBlender "code", "molecule", { code: code, heading: { text: "Javascript Share dialog" } }

        -##
        -# Link - share dialog
        -##
        - @code = link_to("https://www.facebook.com/dialog/share?mobile_iframe=true&app_id=253027968192447&display=popup&href=https://codeblender.net&redirect_uri=https://codeblender.net") do

            -# Icon
            = codeBlender "icon", "atom", { icon: "fa-facebook-official fa-fw fa-3x" }

        :ruby
            code = %{@code}

        -# Code
        = codeBlender "code", "molecule", { code: code, heading: { text: "Link Share dialog" } }

---

meta :
    author      : "DryKISS"
    description : "Popover contextual information - CodeBlender"
    image       : "Image"
    subject     : "DryKISS"
    title       : "Popover contextual information - CodeBlender"
    type        : "website"

heading :
    small : "Provides contextual information for a user."
    title : "Popover"

see :
    - - "Bootstrap popover"
      - "http://getbootstrap.com/javascript/#popovers"
    - - "Angular UI popover"
      - "https://angular-ui.github.io/bootstrap/#/popover"
    - - "Click over"
      - "http://www.leecarmichael.com/bootstrapx-clickover/examples.html"

todo :
    - - ""

locals :
    - - "content"
      - "string"
      - "required"
      - "Button Text"
    - - "placement"
      - "string"
      - "optional"
      - "Left, right, top, bottom, default is auto"
    - - "popBody"
      - "string"
      - "required"
      - "Body of the popover"
    - - "popTitle"
      - "string"
      - "required"
      - "Title of the popover"

---

-# ID
- id = 0

%p
    Uses the dismissible markup over the button tag, coupled with the focus trigger
    this will close pop-ups on a click away.

%p
    When "auto" is specified, it will dynamically reorient the popover.
    For example, if placement is "auto left", the popover will display to the
    left when possible, otherwise it will display right.

-# Default
:ruby
    code = %{partial "./molecule/popover/popover",
                      locals: { content:   "Default",
                                placement: "auto top",
                                popBody:   "Popover Body Copy",
                                popTitle:  "Popover Default" }}

-# Code
= partial "./organism/code/code", locals: { id: id += 1, code: code, heading: { small: "Auto positioned popover", text: "Default Popover" } }

-# Right
:ruby
    code = %{partial "./molecule/popover/popover",
                      locals: { content:   "Right",
                                placement: "right",
                                popBody:   "Popover Body Copy",
                                popTitle:  "Popover Right" }}

-# Code
= partial "./organism/code/code", locals: { id: id += 1, code: code, heading: { text: "Popover Right" } }
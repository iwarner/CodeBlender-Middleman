---

stub : navbar

heading :
    small : "Navigation component"
    title : "Navbar"

/ navigation :
/     - :text : My Offers
/       :url  : /offers
/     - :text : My Bookmarks
/       :url  : /bookmarks
/     - :text : Offer Activity
/       :url  : /activity
/     - :text : Account Settings
/       :url  : /settings

navigation1 :
    - :image  : "site/.png"
      :status : active

see :
    - - "Bootstrap Navbar"
      - "http://getbootstrap.com/component/#navbar"

todo :
    - - "General images and icons need to work for each item and as an item in there own right"
      - "Navbar"
      - "2014-02-15"
      - { "context" : "warning" }
    - - "When multiple items are floated right then they cross over - need to batch these up in one section - right or left in the YAML"
      - "Navbar"
      - "2014-02-15"
      - { "context" : "warning" }
    - - "Need the ability to add images as placeholders."
      - "Navbar"
      - "2014-02-15"
      - { "context" : "info" }
    - - "Active links for dropdown menus needs sorting"
      - "Navbar"
      - "2014-03-06"
      - { "context" : "danger" }

locals :
    - - "array"
      - "array"
      - "optional"
      - "Either define in a YAML or pass in as a local"
    - - "brand"
      - "string"
      - "optional"
      - "Will default to using the config"
    - - "fluid"
      - "bool"
      - "optional"
      - "Mainly used for examples to place the navbar in a designated container"
    - - "logo"
      - "bool"
      - "optional"
      - "Whether to show a logo instead of the brand name"
    - - "position"
      - "string"
      - "optional"
      - "Either fixed-top, fixed-bottom, static-top"
    - - "target"
      - "string"
      - "optional"
      - "Target for the collapse"
    - - "type"
      - "string"
      - "optional"
      - "Inverse or false for default"
    - - "ulClass"
      - "string"
      - "optional"
      - "Class such as navbar-right"
    - - "wrapper"
      - "bool"
      - "optional"
      - "Whether the wrap the Navbar in a class"

---

%p
    JavaScript is required for the Nav Collapse
    Be sure to add a role="navigation" to the navbar to help with accessibility.

    Requires a /data/navigation.yaml file to be present. Basically checks the
    YAML for Divider, Dropdown or Menu Item then displays according to the YAML.

/ -# Default
/ :ruby
/     code = %{partial "./bootstrap/component/navbar",
/                       locals: { fluid:    true,
/                                 position: "static-top" }}

/ -# Code
/ = partial "./organism/code/code",
/            locals: { id:      1,
/                      code:    code,
/                      heading: { text: "Default" } }

/ -# Logo
/ :ruby
/     code = %{partial "./bootstrap/component/navbar",
/                       locals: { array:    current_page.data.navigation1,
/                                 fluid:    true,
/                                 logo:     true,
/                                 position: "static-top" }}

/ -# Code
/ = partial "./organism/code/code",
/            locals: { id:      2,
/                      code:    code,
/                      heading: { text: "Logo" } }

/ -# Inverted
/ :ruby
/     code = %{partial "./bootstrap/component/navbar",
/                       locals: { array:    current_page.data.navigation,
/                                 brand:    "Test NavBar",
/                                 fluid:    true,
/                                 position: "static-top",
/                                 type:     "inverse" }}

/ -# Code
/ = partial "./organism/code/code",
/            locals: { id:      3,
/                      code:    code,
/                      heading: { text: "Inverted" } }
-##
-# Alert
-#
-# @author   Ian Warner <ian.warner@drykiss.com>
-# @category molecule
-##

-# Variable
- extraClass = locals[ :context ] ? "alert-#{ locals[ :context ] }" : "alert-success"
- ng         = locals[ :ng ]      ? locals[ :ng ]                   : false

-# Alert
.alert{ class: extraClass, ng: ng }

    -# Dismissible
    - if locals[ :dismiss ]

        -# Button
        = codeBlender "button", "atom", { aria: { label: "Close" }, text: "&times;", data: { dismiss: "alert" }, extraClass: "close", type: "button" }

    -# Text
    = string locals[ :text ]
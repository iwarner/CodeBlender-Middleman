-##
-# Blog Image
-#
-# @usage
-# -# Blog Image
-# = partial "molecule/image/image.haml"
-#
-# @category molecule
-# @author   Ian Warner <ian.warner@drykiss.com>
-##

-# Variables
- cp    = current_page.data
- image = cp.meta.image
- title = cp.title

-# Check if current page has an image
- if image

    -# Look for an array
    - if image.is_a?( Array )

        -# Carousel
        = partial "molecule/carousel/carousel.haml", locals: { id: "carousel1", array: image, indicator: :true }

    - elsif ! image.is_a?( Array )

        -# Image
        = partial "atom/image/image.haml", locals: { alt: title, src: image, extraClass: "img-responsive" }
---

# Angular
angular :
    controller   : SearchInputController
    controllerAs : searchInput

description : "Search input"
title       : "Search input"

# See
see :
    - - "Bootstrap search addons"
      - "http://getbootstrap.com/components/#input-groups-buttons"
    - - "Search usability"
      - "https://uxmag.com/articles/usability-tip-dont-make-me-search-for-search"

# Todo
todo :
    - - "Include typeahead"
    - - "Validation"
    - - "Icons to show typeahead is thinking"
    - - "Ability to only allow search once typeahead is selected"
    - - "Prepend with button filter for selection"

---

-# Controller loaded
%span.controllerLoaded{ ng: { if: "searchInput.controllerLoaded" } }

    -# Icon
    = codeBlender "icon", "atom", { icon: "fa-code" }

-# Search input
:ruby
    code = %{codeBlender "search-input", "molecule"}

-# Code
= codeBlender "code", "molecule", { code: code, heading: { text: "Example" } }

%hr

-# Bootstrap row
.row

    -# Bootstrap column
    .col-md-6

        -# Panel body - markdown
        - panelBody = capture_html do
            :markdown

                - Angular directive to control all aspects of this component
                - Typeahead option enabled using [Angular UI Typeahead](https://angular-ui.github.io/bootstrap/#/typeahead)
                - Refresh icon appears when utilising the typeahead

        -# Panel
        = codeBlender "panel", "molecule", { body: panelBody, heading: { text: "Features" } }

    -# Bootstrap column
    .col-md-6

        -# Panel body - markdown
        - panelBody = capture_html do
            :markdown

                Place the following inside your locale file

                ~~~
                # Search
                search-input :
                    button :
                        text : "Search"
                    input:
                        placeholder : "Search"
                    label :
                        label : "Search"
                ~~~

        -# Panel
        = codeBlender "panel", "molecule", { body: panelBody, heading: { text: "Translation" } }

%hr

-# Bootstrap row
.row

    -# Bootstrap column
    .col-md-6

        -# Panel body - markdown
        - panelBody = capture_html do
            :markdown

                Utilises the button and input partials

        -# Panel
        = codeBlender "panel", "molecule", { body: panelBody, heading: { text: "Hierarchy" } }

    -# Bootstrap column
    .col-md-6

        -# Panel body - markdown
        - panelBody = capture_html do
            :markdown

                HERE

        -# Panel
        = codeBlender "panel", "molecule", { body: panelBody, heading: { text: "HERE" } }

%hr
